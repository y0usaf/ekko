{
  "checks": [
    {
      "command": "nix flake check --keep-going -L .",
      "exit_code": 0,
      "log": "/tmp/ekko-desktop-complete-check.log"
    },
    {
      "command": "nix build --no-link -L .#checks.x86_64-linux.desktop-menus",
      "exit_code": 0,
      "log": "/tmp/ekko-overlap-check.log",
      "includes": [
        "default and bare profiles",
        "three startup windows",
        "menus and keyboard navigation",
        "snap and swap",
        "floating move and resize",
        "cancel",
        "reconnect preserves placement and PIDs",
        "click-through isolation"
      ]
    }
  ],
  "limitations": [
    "Animation is a cell outline; transient strokes avoid image content. Application geometry commits on release."
  ]
}
