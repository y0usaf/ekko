# Invalid startup query ordering probe: second attempt

This directory is retained as raw evidence only. The second attempt used the
wrong output path and did not produce an accepted startup-order experiment.
It must not support a conclusion. The JSON and ANSI payloads below are
compressed with reproducible gzip; their contents are unchanged.
